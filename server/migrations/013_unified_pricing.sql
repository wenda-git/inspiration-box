-- =========================================================
-- Migration 013: 统一定价（C 方案）
-- 三档方向仍存在（算法需要），但价格对齐，不再做价格差异
-- =========================================================

UPDATE subscription_plans SET price_cny = 128, budget_cny = 128;
